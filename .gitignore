__pycache__
out
# The Go client lives in the bk_client submodule; its build outputs are ignored
# there. Locally built binaries also land under out/ (ignored above).
client_builds
.mypy_cache
.venv
.pdm-python

.ruff_cache

# Local test reports are generated, not source files.
.coverage*
coverage*.xml
htmlcov/

# generated vendor directory (populated by `python dev.py vendor` or `python dev.py build`)
bk_maya/lib/
# guard against an accidental nested copy if dev.py is ever run from the wrong cwd
**/bk_maya/bk_maya/

# generated version stamp (written into builds by `python dev.py build`)
bk_maya/_build_version.py

# prevent publishing of dynamically created updater cache
*_updater_status.json

# debug and backup folders
_debug/
_bck/

# debug env variables
.env

# macos thumbnail database
.DS_Store

# man
instructions.txt
